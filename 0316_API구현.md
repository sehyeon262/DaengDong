# 0316 API 구현 정리

## 구현 기능
- GPS 트래킹 (좌표 수집 및 Redis 저장)
- 산책 거리 계산 (Haversine 공식)
- 소모 칼로리 표시 (체중 × 거리 × 0.8)

## 브랜치
`feat/S14P21E108-107-walk-route-tracking`

## 구현한 API 3개

| 메서드 | URL | 설명 |
|---|---|---|
| POST | `/api/v1/walks/{walkId}/locations` | GPS 좌표 배치 저장 |
| GET  | `/api/v1/walks/{walkId}/distance`  | 누적 거리 조회 |
| GET  | `/api/v1/walks/{walkId}/calories`  | 소모 칼로리 조회 |

---

## 전체 흐름

```
FE (앱) → GPS 좌표 전송 → Redis에 버퍼링 (산책 중)
                               ↓
               거리/칼로리 조회 시 Redis에서 좌표 읽어 계산
                               ↓
               산책 종료 시 → PostgreSQL walk_records에 최종 저장
```

---

## 생성/수정 파일 목록

### 1. `build.gradle` — 의존성 추가
프로젝트에서 사용할 외부 라이브러리를 등록하는 파일.
아래 2개를 추가했다.

- `spring-boot-starter-data-redis`
  - Spring에서 Redis를 사용하기 위한 라이브러리
  - GPS 좌표를 산책 중에 Redis에 임시 저장(버퍼링)하기 위해 필요
- `org.hibernate.orm:hibernate-spatial`
  - PostgreSQL의 PostGIS 지원 라이브러리
  - `GEOGRAPHY(LINESTRING, 4326)` 타입으로 GPS 경로를 DB에 저장하기 위해 필요

---

### 2. `src/main/resources/application-dev.yaml` — Redis 연결 설정 추가
개발 환경에서 사용하는 설정 파일.
Spring이 Redis 서버에 접속할 수 있도록 주소와 포트를 추가했다.

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
```

docker-compose에서 Redis가 `localhost:6379`로 실행되기 때문에 이 값으로 설정.

---

### 3. `global/config/RedisConfig.java` — Redis Bean 등록
Spring 애플리케이션에서 Redis를 실제로 사용하려면 `RedisTemplate` 객체를 Bean으로 등록해야 한다.
이 파일이 그 역할을 한다.

- `RedisConnectionFactory`: Redis 서버와의 실제 연결을 담당
- `RedisTemplate<String, String>`: Redis에 문자열 키/값으로 데이터를 읽고 쓰는 객체
- 서비스에서 `@Autowired` 없이 생성자 주입으로 사용 가능하게 해준다

---

### 4. `domain/walk/entity/WalkStatus.java` — 산책 상태 enum
`walk_records.walk_status` 컬럼에 저장되는 문자열 값을 Java 코드에서 타입으로 관리하기 위한 열거형.
오타 방지와 IDE 자동완성 지원이 목적이다.

```
IN_PROGRESS  → 산책 진행 중
COMPLETED    → 산책 완료
CANCELLED    → 산책 취소
```

---

### 5. `domain/walk/entity/WalkRecord.java` — walk_records 테이블 매핑 엔티티
DB의 `walk_records` 테이블과 1:1로 매핑되는 JPA 엔티티 클래스.
이 클래스를 통해 Java 코드에서 DB를 읽고 쓸 수 있다.

주요 필드:
| 필드 | DB 컬럼 | 설명 |
|---|---|---|
| `dogId` | `dog_id2` | 반려견 ID (ERD 툴 네이밍 이슈로 dog_id2) |
| `walkStatus` | `walk_status` | 산책 상태 (WalkStatus enum) |
| `startTime` / `endTime` | `start_time` / `end_time` | 산책 시작/종료 시각 |
| `totalDistance` | `total_distance` | 총 이동 거리 (단위: m) |
| `routeLine` | `route_line` | GPS 경로 선 (GEOGRAPHY(LINESTRING, 4326)) |
| `calories` | `calories` | 소모 칼로리 (단위: kcal) |

> ⚠️ `route_line` 컬럼 타입을 ERD의 `VARCHAR(255)`에서 `GEOGRAPHY(LINESTRING, 4326)`으로 변경.
> `ddl-auto: update` 설정 때문에 처음 서버 실행 시 테이블을 drop 후 재생성하거나 수동 ALTER 필요.

---

### 6. `domain/dog/entity/Dog.java` — dogs 테이블 (읽기 전용)
칼로리 계산에 필요한 반려견 체중(`weight`)을 조회하기 위한 최소 엔티티.
Dog 도메인 전체 구현은 담당 팀원이 별도 진행 예정이며, 이 파일은 `weight` 필드 조회 목적으로만 사용한다.

---

### 7. `domain/walk/repository/WalkRecordRepository.java` — walk_records DB 접근
`WalkRecord` 엔티티를 DB에서 조회/저장하는 인터페이스.
Spring Data JPA가 인터페이스 선언만 보고 구현체를 자동 생성해준다.
Service에서 `walkRecordRepository.findById(walkId)` 형태로 사용한다.

---

### 8. `domain/dog/repository/DogRepository.java` — dogs DB 접근
`Dog` 엔티티를 DB에서 조회하는 인터페이스.
칼로리 계산 시 `dogRepository.findById(dogId)`로 체중을 가져오기 위해 사용한다.

---

### 9-1. `domain/walk/dto/request/LocationBatchRequest.java` — GPS 좌표 요청 DTO
FE가 BE로 GPS 좌표를 전송할 때 사용하는 요청 데이터 형식.
한 번에 여러 좌표를 배치로 보낼 수 있다.

```json
{
  "locations": [
    {"latitude": 37.5665, "longitude": 126.9780, "timestamp": 1234567890000},
    {"latitude": 37.5670, "longitude": 126.9785, "timestamp": 1234567895000}
  ]
}
```

---

### 9-2. `domain/walk/dto/response/DistanceResponse.java` — 거리 응답 DTO
`GET /distance` API의 응답 데이터 형식.

```json
{"distanceM": 1234.56, "distanceKm": 1.23}
```

---

### 9-3. `domain/walk/dto/response/CaloriesResponse.java` — 칼로리 응답 DTO
`GET /calories` API의 응답 데이터 형식.
체중 미입력 시 `requiresWeight: true`를 반환해 FE에서 입력 유도 UI를 표시하게 한다.

```json
// 체중 있을 때
{"calories": 45.6, "requiresWeight": false}

// 체중 미입력 시
{"requiresWeight": true}
```

---

### 10. `domain/walk/exception/WalkNotFoundException.java` — 404 예외
산책 세션을 찾을 수 없을 때 던지는 예외 클래스.
기존 프로젝트 패턴인 `NotFoundGroupException`을 상속해서, `ControllerAdvice`가 자동으로 404 응답을 반환하게 한다.

```java
throw new WalkNotFoundException(); // → HTTP 404 자동 반환
```

---

### 11. `domain/walk/service/WalkService.java` — 비즈니스 로직
실제 기능 로직이 구현된 핵심 클래스. Controller는 요청만 받고, 모든 처리는 여기서 한다.

**saveLocations (GPS 좌표 저장)**
- Redis 키 형식: `walk:gps:{walkId}`
- 좌표를 `"위도,경도,타임스탬프"` 형식의 문자열로 Redis List에 순서대로 저장
- 산책이 끝날 때까지 좌표가 쌓임

**getDistance (거리 계산)**
- Redis에서 모든 좌표를 읽어와 인접 좌표 간 거리를 Haversine 공식으로 계산
- Haversine: 지구가 둥근 것을 고려한 두 위경도 사이의 실제 거리 계산 공식

**getCalories (칼로리 계산)**
- 공식: `체중(kg) × 거리(km) × 0.8`
- 체중이 null이면 `requiresWeight: true` 반환

---

### 12. `domain/walk/controller/WalkController.java` — API 진입점
FE의 HTTP 요청을 받아서 Service에 위임하고, 결과를 JSON으로 반환하는 클래스.
비즈니스 로직은 없고 요청 전달과 응답 포맷팅만 담당한다.

---

## 기술 결정 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| GPS 임시 저장 | Redis List | 산책 중 실시간 수집, 종료 시 PostgreSQL로 이동 |
| 경로 저장 타입 | `GEOGRAPHY(LINESTRING, 4326)` | PostGIS 네이티브 타입, 팀에서 이미 places 테이블에 사용 중 |
| 거리 계산 방식 | Haversine 공식 (BE 계산) | 지구 곡률 반영한 정확한 거리 |
| 칼로리 계산식 | `체중 × 거리km × 0.8` | 체중+거리 기반 간소화 공식 |
| 좌표 수집 주기 | 5초 (FE 구현 시 적용 예정) | 정확도와 배터리 소모의 균형 |

---

## 다음 단계
1. Docker로 로컬 테스트 (PostgreSQL + Redis 실행 후 Postman으로 API 확인)
2. FE: GPS 수집 + API 연동 구현
3. FE + BE 통합 테스트
